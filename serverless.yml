service: loggly-slack-alerts-nodejs

provider:
  name: aws
  runtime: nodejs6.10

functions:
  alert:
    handler: handler.alert

    events:
      - http:
          path: /alert
          method: post
